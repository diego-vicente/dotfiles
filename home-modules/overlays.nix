[
  # Overlays for the home-manager configuration. Take into account that these
  # overlays only apply if the program is directly managed by home-manager (i.e
  # `programs.alacritty.enable = true`); NOT programs that are installed via
  # home-manager, whose overlays should be in nixos-modules/overlays.nix
]